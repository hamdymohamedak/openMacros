# Installation

Add the crate to your Rust project:

```toml
[dependencies]
ak_macros = "1"
```

## Requirements

- Rust edition: `2021`
- Recommended Rust toolchain: stable

## Optional Docs Commands

This repository ships VitePress docs. From project root:

```bash
npm install
npm run docs:dev
```
